/**
 * Web OAuth client from Google Cloud project **shoesrus** (same project as Android client).
 * Must match the Google Sign-In / Firebase project client used by the mobile app.
 * If the backend verifies Google ID tokens, its GOOGLE_CLIENT_ID must be updated too.
 */
export const GOOGLE_WEB_CLIENT_ID =
  '45503149806-4gl1n00c5autjkatq73cs9t4g6ooshej.apps.googleusercontent.com';

/** Google Cloud project ID (console selector). */
export const GOOGLE_CLOUD_PROJECT = 'shoesrus';
