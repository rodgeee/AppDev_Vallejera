/**
 * Shoes R' Us API — production (Railway).
 * Shop, orders, profile, and images all use this base URL.
 */
export const API_BASE_URL = 'https://finalwebsitesru-production.up.railway.app';

export { GOOGLE_CLOUD_PROJECT, GOOGLE_WEB_CLIENT_ID } from './google.config';
